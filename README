WoW Addon - Enables auto-comparision of equipment when hovering

This addon activates builtin Blizzard console switches and therefore uses no ram/cpu resources and has no bad interactions with other addons.




# curse auto-build
git commit -a || echo "did you forget to bump TOC?"
git tag  #to see last tags
git tag -a v3.0 -m "Release 3.0"
git push --follow-tags
#git push && git push origin tag $(git tag |tail -1)
